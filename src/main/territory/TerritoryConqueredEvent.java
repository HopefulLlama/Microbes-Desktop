package main.territory;

import java.util.EventObject;

public class TerritoryConqueredEvent extends EventObject {
    public TerritoryConqueredEvent(Object source) {
        super(source);
    }
}
